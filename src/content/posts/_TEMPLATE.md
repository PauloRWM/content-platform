---
# ── Copy this file to start a new post. ────────────────────────────────
# In the GitHub browser editor: open this file, click "Copy raw file", then
# create a new file at  src/content/posts/your-title-here.md  and paste.
# On a computer: run  npm run new:post "Your Title Here"  and it does this for you.
#
# Fill in the fields below, then write your article under the second "---" line.
# Anything after a "#" on these lines is a note to you and is ignored.

title: "Your headline goes here"
description: "One or two sentences that summarize the post (used for SEO + previews)."
pubDate: 2026-01-01            # The date this post is published. Format: YYYY-MM-DD.
author: "paulo testes"        # Optional. Delete this line to use the default.
tags: []                      # Optional, e.g. tags: ["how-to", "product"]

# ── The publish switch ─────────────────────────────────────────────────
# Leave this as `true` while writing and during review. It keeps the post OFF
# the live site. Change it to `false` only when the post is approved and ready
# to go live — that (plus merging to `main`) is what publishes it.
draft: true
---

Write your post here using Markdown.

## A subheading

A paragraph of text. You can use **bold**, *italics*, [links](https://example.com),
and lists:

- point one
- point two

> A blockquote for a pull-quote or callout.

That's it — save the file, open a pull request, and follow the review steps in the README.
